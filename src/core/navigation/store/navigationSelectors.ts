/**
 * Navigation Store Selectors
 *
 * Read-only access patterns for the navigation graph.
 * Components use these selectors instead of direct store access.
 *
 * @module navigationSelectors
 */

// Placeholder: Selectors will be defined as the architecture matures
export const navigationSelectors = {
  // TODO: Add typed selectors for common read patterns
};
