/**
 * Debug Configuration
 *
 * Change these values to configure debug behavior.
 * These settings only affect development - they are ignored in production builds.
 */

export const debugConfig = {
  /**
   * Starting slide index (0-based)
   *
   * Set this to skip to a specific slide when the app loads.
   * Set to 0 or null to start from the beginning.
   *
   * Examples:
   *   0  - Start from the first slide (default)
   *   5  - Start from slide 6 (0-indexed)
   *   10 - Start from slide 11
   */
  startingSlideIndex: 0,
};
